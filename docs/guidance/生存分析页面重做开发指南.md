# 生存分析页面重做开发指南

## 概述

本文档描述了如何重做生存分析页面，将传统的柱状图分析改为点阵可视化，并实现多维度过滤器。新的设计将提供更直观的数据探索体验，让用户能够以点的形式查看每个乘客的生存状态，并通过多维度过滤器进行精细筛选。

## 功能需求

### 1. 点阵可视化

#### 数据接口定义
```typescript
// 乘客数据接口
interface Passenger {
  PassengerId: number;
  Survived: number; // 0: 死亡, 1: 生存
  Pclass: number; // 舱位等级: 1, 2, 3
  Name: string;
  Sex: string; // 'male' | 'female'
  Age_original?: number; // 原始年龄
  SibSp: number; // 兄弟姐妹/配偶数量
  Parch: number; // 父母/子女数量
  Fare?: number; // 票价
  Embarked?: string; // 登船港口: 'C', 'Q', 'S'
}

// 点阵数据点接口
interface ScatterPoint {
  passenger: Passenger;
  x: number; // X坐标
  y: number; // Y坐标
  size: number; // 点的大小
  isFiltered: boolean; // 是否被筛选选中
  clusterId?: number; // 聚类ID（可选）
}

// 点阵可视化组件Props
interface PassengerScatterPlotProps {
  passengers: Passenger[];
  filteredPassengerIds?: number[]; // 被筛选选中的乘客ID列表
  width: number;
  height: number;
  layoutType?: 'grid' | 'cluster'; // 布局类型
  onPassengerHover?: (passenger: Passenger | null) => void;
  onPassengerClick?: (passenger: Passenger) => void;
  onPassengerSelect?: (passengerIds: number[]) => void; // 批量选择
}
```

#### 可视化需求详情

- **均匀分布布局**：将每个乘客以点的形式均匀铺在页面中
- **筛选状态标识**：
  - 被筛选选中的乘客：显示彩色（生存：绿色，死亡：红色），可交互
  - 未被筛选选中的乘客：显示灰色且不可点击
- **生存状态标识**：用不同颜色区分生存（绿色）和死亡（红色）的乘客
- **交互功能**：
  - 鼠标悬停显示乘客详细信息（仅对筛选选中的乘客有效）
  - 点击选中单个乘客（仅对筛选选中的乘客有效）
  - 框选多个乘客进行批量操作（仅对筛选选中的乘客有效）

#### 布局实现优先级
1. **第一阶段**：实现均匀分布布局（grid）
2. **第二阶段**：可选实现数据相似度聚类布局（cluster）

### 2. 多维度过滤器
参考淘宝搜索栏的商品筛选器，实现以下维度的过滤：

#### 基础维度
- **生存状态**：生存/死亡/全部
- **性别**：男性/女性/全部
- **舱位等级**：一等舱/二等舱/三等舱/全部

#### 人口统计维度
- **年龄分组**：儿童(0-12岁)/青少年(13-17岁)/成人(18-59岁)/老人(60+岁)/全部
- **家庭规模**：独自旅行/小型家庭(2-3人)/大型家庭(4+人)/全部

#### 社会经济维度
- **票价分组**：低价票(0-10)/中等票价(10-30)/高价票(30+)/全部
- **登船港口**：瑟堡/皇后镇/南安普顿/全部

#### 高级筛选
- **年龄范围滑块**：可拖动选择年龄范围
- **票价范围滑块**：可拖动选择票价范围
- **搜索框**：支持按姓名模糊搜索

### 3. 过滤器交互特性
- **多选支持**：每个维度支持多选
- **即时过滤**：选择过滤器后实时更新点阵显示
- **筛选结果统计**：显示当前筛选条件下的乘客总数和生存率
- **清除筛选**：一键清除所有筛选条件
- **筛选历史**：支持撤销/重做筛选操作

### 4. 实时生存率统计
- **当前条件生存率**：实时计算并显示当前筛选条件下的生存率
- **对比基准**：显示当前生存率与总体生存率的对比
- **统计指标**：
  - 筛选后乘客总数
  - 生存人数
  - 死亡人数
  - 生存率百分比
  - 与总体生存率的差异
- **可视化展示**：使用进度条或环形图直观展示生存率

#### 统计计算逻辑
```typescript
// 计算当前筛选条件下的生存率
const calculateSurvivalRate = (passengers: Passenger[], filteredIds: number[]) => {
  const filteredPassengers = passengers.filter(p => 
    filteredIds.includes(p.PassengerId)
  );
  
  if (filteredPassengers.length === 0) return 0;
  
  const survivedCount = filteredPassengers.filter(p => p.Survived).length;
  return (survivedCount / filteredPassengers.length) * 100;
};

// 计算总体生存率（基准）
const calculateOverallSurvivalRate = (passengers: Passenger[]) => {
  const survivedCount = passengers.filter(p => p.Survived).length;
  return (survivedCount / passengers.length) * 100;
};
```

#### 统计指标组件
```typescript
interface SurvivalStatsProps {
  totalPassengers: number;
  survivedCount: number;
  deathCount: number;
  survivalRate: number;
  overallSurvivalRate: number;
}

const SurvivalStats: React.FC<SurvivalStatsProps> = ({
  totalPassengers,
  survivedCount,
  deathCount,
  survivalRate,
  overallSurvivalRate
}) => {
  const difference = survivalRate - overallSurvivalRate;
  
  return (
    <div className="survival-stats">
      <div className="stats-grid">
        <div className="stat-item">
          <span className="stat-label">筛选后总数</span>
          <span className="stat-value">{totalPassengers}</span>
        </div>
        <div className="stat-item">
          <span className="stat-label">生存人数</span>
          <span className="stat-value survived">{survivedCount}</span>
        </div>
        <div className="stat-item">
          <span className="stat-label">死亡人数</span>
          <span className="stat-value death">{deathCount}</span>
        </div>
        <div className="stat-item">
          <span className="stat-label">当前生存率</span>
          <span className="stat-value rate">{survivalRate.toFixed(1)}%</span>
        </div>
        <div className="stat-item">
          <span className="stat-label">与总体对比</span>
          <span className={`stat-value ${difference >= 0 ? 'positive' : 'negative'}`}>
            {difference >= 0 ? '+' : ''}{difference.toFixed(1)}%
          </span>
        </div>
      </div>
      
      {/* 生存率可视化进度条 */}
      <div className="survival-rate-bar">
        <div className="rate-labels">
          <span>0%</span>
          <span>当前生存率: {survivalRate.toFixed(1)}%</span>
          <span>总体生存率: {overallSurvivalRate.toFixed(1)}%</span>
          <span>100%</span>
        </div>
        <div className="bar-container">
          <div 
            className="overall-rate-marker" 
            style={{ left: `${overallSurvivalRate}%` }}
            title={`总体生存率: ${overallSurvivalRate.toFixed(1)}%`}
          ></div>
          <div 
            className="current-rate-fill" 
            style={{ width: `${survivalRate}%` }}
          ></div>
        </div>
      </div>
    </div>
  );
};
```

#### 样式设计
```css
.survival-stats {
  background: #f8fafc;
  border: 1px solid #e2e8f0;
  border-radius: 8px;
  padding: 16px;
  margin-bottom: 16px;
}

.stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(120px, 1fr));
  gap: 12px;
  margin-bottom: 16px;
}

.stat-item {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.stat-label {
  font-size: 0.875rem;
  color: #64748b;
  margin-bottom: 4px;
}

.stat-value {
  font-size: 1.25rem;
  font-weight: 600;
}

.stat-value.survived { color: #10b981; }
.stat-value.death { color: #ef4444; }
.stat-value.rate { color: #3b82f6; }
.stat-value.positive { color: #10b981; }
.stat-value.negative { color: #ef4444; }

.survival-rate-bar {
  margin-top: 12px;
}

.rate-labels {
  display: flex;
  justify-content: space-between;
  font-size: 0.75rem;
  color: #64748b;
  margin-bottom: 8px;
}

.bar-container {
  position: relative;
  height: 8px;
  background: #e2e8f0;
  border-radius: 4px;
  overflow: hidden;
}

.current-rate-fill {
  height: 100%;
  background: linear-gradient(90deg, #ef4444, #f59e0b, #10b981);
  transition: width 0.3s ease;
}

.overall-rate-marker {
  position: absolute;
  top: -2px;
  width: 2px;
  height: 12px;
  background: #374151;
  transform: translateX(-50%);
}
```

## 技术实现方案

### 1. 组件架构设计

```
SurvivalAnalysisPage/
├── components/
│   ├── PassengerScatterPlot/          # 乘客点阵可视化组件
│   │   ├── PassengerScatterPlot.tsx
│   │   ├── useScatterLayout.ts        # 点阵布局算法
│   │   └── PassengerTooltip.tsx       # 乘客信息提示框
│   ├── FilterPanel/                   # 过滤器面板
│   │   ├── FilterPanel.tsx
│   │   ├── FilterSection.tsx          # 单个过滤器区域
│   │   ├── RangeSlider.tsx            # 范围滑块组件
│   │   └── FilterChips.tsx            # 筛选标签显示
│   └── StatisticsPanel/               # 统计信息面板
│       ├── StatisticsPanel.tsx
│       └── SurvivalMetrics.tsx
├── hooks/
│   ├── usePassengerFilter.ts          # 乘客数据过滤逻辑
│   └── useFilterState.ts              # 过滤器状态管理
└── types/
    └── filterTypes.ts                 # 过滤器相关类型定义
```

### 2. 点阵布局算法

#### 均匀分布算法
```typescript
// 使用网格布局均匀分布点
const createGridLayout = (data: Passenger[], containerWidth: number, containerHeight: number) => {
  const pointsPerRow = Math.ceil(Math.sqrt(data.length));
  const spacingX = containerWidth / (pointsPerRow + 1);
  const spacingY = containerHeight / (pointsPerRow + 1);
  
  return data.map((passenger, index) => {
    const row = Math.floor(index / pointsPerRow);
    const col = index % pointsPerRow;
    
    return {
      passenger,
      x: (col + 0.5) * spacingX,
      y: (row + 0.5) * spacingY,
      size: 6 // 点的大小
    };
  });
};
```

#### 相似度聚类算法（可选）

**性能考虑与优化方案**

聚类算法在前端实时计算确实可能带来较大的性能开销，特别是对于K-means或层次聚类等复杂算法。以下是优化方案：

##### 方案一：Python预处理（推荐）
```python
# 在数据预处理阶段计算聚类结果
import pandas as pd
import numpy as np
from sklearn.cluster import KMeans
from sklearn.preprocessing import StandardScaler

def precompute_clusters():
    # 加载泰坦尼克号数据集
    df = pd.read_csv('data/raw/Titanic-Dataset.csv')
    
    # 选择聚类特征
    features = ['Pclass', 'Age', 'Fare', 'SibSp', 'Parch']
    
    # 处理缺失值
    df_clean = df[features].fillna({
        'Age': df['Age'].median(),
        'Fare': df['Fare'].median()
    })
    
    # 标准化特征
    scaler = StandardScaler()
    scaled_features = scaler.fit_transform(df_clean)
    
    # 使用K-means聚类
    kmeans = KMeans(n_clusters=5, random_state=42)
    clusters = kmeans.fit_predict(scaled_features)
    
    # 保存聚类结果到JSON文件
    df['cluster'] = clusters
    cluster_data = df[['PassengerId', 'cluster']].to_dict('records')
    
    # 保存到前端可访问的文件
    with open('src/data/passenger_clusters.json', 'w') as f:
        json.dump(cluster_data, f, indent=2)
    
    print(f"聚类完成，共生成{len(np.unique(clusters))}个簇")
```

##### 方案二：前端简化聚类
```typescript
// 使用简化的基于网格的聚类，避免复杂计算
const createSimpleClusterLayout = (data: Passenger[], containerWidth: number, containerHeight: number) => {
  // 基于特征值的简单分组
  const groups = data.reduce((acc, passenger) => {
    const groupKey = `${passenger.Pclass}-${Math.floor(passenger.Age_original || 0 / 10)}-${Math.floor(passenger.Fare || 0 / 20)}`;
    if (!acc[groupKey]) acc[groupKey] = [];
    acc[groupKey].push(passenger);
    return acc;
  }, {} as Record<string, Passenger[]>);
  
  // 在容器内均匀分布各组
  const groupKeys = Object.keys(groups);
  const groupsPerRow = Math.ceil(Math.sqrt(groupKeys.length));
  
  return groupKeys.flatMap((key, groupIndex) => {
    const group = groups[key];
    const row = Math.floor(groupIndex / groupsPerRow);
    const col = groupIndex % groupsPerRow;
    
    const groupX = (col + 0.5) * (containerWidth / groupsPerRow);
    const groupY = (row + 0.5) * (containerHeight / groupsPerRow);
    const groupRadius = Math.min(containerWidth, containerHeight) / (groupsPerRow * 3);
    
    // 在组内均匀分布点
    return group.map((passenger, pointIndex) => {
      const angle = (pointIndex / group.length) * 2 * Math.PI;
      const radius = groupRadius * (pointIndex === 0 ? 0 : 0.8); // 第一个点在中心
      
      return {
        passenger,
        x: groupX + Math.cos(angle) * radius,
        y: groupY + Math.sin(angle) * radius,
        size: 6,
        group: key
      };
    });
  });
};
```

##### 方案三：Web Worker并行计算
```typescript
// 在Web Worker中执行聚类计算，避免阻塞主线程
const useClusterWorker = (data: Passenger[]) => {
  const [clusters, setClusters] = useState<ClusterPoint[]>([]);
  
  useEffect(() => {
    const worker = new Worker(new URL('../workers/clusterWorker.ts', import.meta.url));
    
    worker.postMessage({ data, method: 'kmeans', k: 5 });
    
    worker.onmessage = (e) => {
      setClusters(e.data.clusters);
    };
    
    return () => worker.terminate();
  }, [data]);
  
  return clusters;
};
```

**推荐方案：Python预处理**

考虑到性能优化和用户体验，**推荐使用Python预处理方案**：

1. **性能优势**：一次性计算，前端直接加载结果
2. **算法灵活性**：可以使用更复杂的聚类算法
3. **数据质量**：可以处理缺失值和异常值
4. **可维护性**：聚类逻辑与前端代码分离

**实施步骤**：
1. 在数据预处理脚本中添加聚类计算
2. 生成包含聚类结果的JSON文件
3. 前端直接加载预计算的聚类数据
4. 根据聚类ID进行可视化布局

### 3. 过滤器状态管理

```typescript
interface FilterState {
  // 基础维度
  survivalStatus: ('survived' | 'died')[];
  gender: ('male' | 'female')[];
  passengerClass: (1 | 2 | 3)[];
  
  // 人口统计维度
  ageGroup: ('child' | 'teen' | 'adult' | 'senior')[];
  familySize: ('solo' | 'small' | 'large')[];
  
  // 社会经济维度
  fareGroup: ('low' | 'medium' | 'high')[];
  embarked: ('C' | 'Q' | 'S')[];
  
  // 高级筛选
  ageRange: [number, number];
  fareRange: [number, number];
  nameSearch: string;
}

const useFilterState = () => {
  const [filters, setFilters] = useState<FilterState>({
    survivalStatus: [],
    gender: [],
    passengerClass: [],
    ageGroup: [],
    familySize: [],
    fareGroup: [],
    embarked: [],
    ageRange: [0, 80],
    fareRange: [0, 512],
    nameSearch: ''
  });
  
  // 更新单个过滤器
  const updateFilter = (filterKey: keyof FilterState, value: any) => {
    setFilters(prev => ({ ...prev, [filterKey]: value }));
  };
  
  // 清除所有过滤器
  const clearAllFilters = () => {
    setFilters({
      survivalStatus: [],
      gender: [],
      passengerClass: [],
      ageGroup: [],
      familySize: [],
      fareGroup: [],
      embarked: [],
      ageRange: [0, 80],
      fareRange: [0, 512],
      nameSearch: ''
    });
  };
  
  return { filters, updateFilter, clearAllFilters };
};
```

### 4. 数据过滤逻辑

```typescript
const usePassengerFilter = (passengers: Passenger[], filters: FilterState) => {
  return useMemo(() => {
    return passengers.filter(passenger => {
      // 生存状态过滤
      if (filters.survivalStatus.length > 0) {
        const isSurvived = passenger.Survived === 1;
        if (!filters.survivalStatus.includes(isSurvived ? 'survived' : 'died')) {
          return false;
        }
      }
      
      // 性别过滤
      if (filters.gender.length > 0) {
        if (!filters.gender.includes(passenger.Sex.toLowerCase() as 'male' | 'female')) {
          return false;
        }
      }
      
      // 舱位等级过滤
      if (filters.passengerClass.length > 0) {
        if (!filters.passengerClass.includes(passenger.Pclass as 1 | 2 | 3)) {
          return false;
        }
      }
      
      // 年龄分组过滤
      if (filters.ageGroup.length > 0) {
        const age = passenger.Age_original || 0;
        let ageGroup: string;
        if (age <= 12) ageGroup = 'child';
        else if (age <= 17) ageGroup = 'teen';
        else if (age <= 59) ageGroup = 'adult';
        else ageGroup = 'senior';
        
        if (!filters.ageGroup.includes(ageGroup as any)) {
          return false;
        }
      }
      
      // 年龄范围过滤
      const age = passenger.Age_original || 0;
      if (age < filters.ageRange[0] || age > filters.ageRange[1]) {
        return false;
      }
      
      // 票价范围过滤
      const fare = passenger.Fare || 0;
      if (fare < filters.fareRange[0] || fare > filters.fareRange[1]) {
        return false;
      }
      
      // 姓名搜索过滤
      if (filters.nameSearch) {
        const searchTerm = filters.nameSearch.toLowerCase();
        const name = passenger.Name?.toLowerCase() || '';
        if (!name.includes(searchTerm)) {
          return false;
        }
      }
      
      return true;
    });
  }, [passengers, filters]);
};
```

### 5. 组件实现细节

#### PassengerScatterPlot 组件实现
```tsx
const PassengerScatterPlot: React.FC<PassengerScatterPlotProps> = ({
  passengers,
  filteredPassengerIds = [],
  width,
  height,
  layoutType = 'grid',
  onPassengerHover,
  onPassengerClick,
  onPassengerSelect
}) => {
  const layout = useScatterLayout(passengers, width, height, layoutType);
  const [hoveredPassenger, setHoveredPassenger] = useState<Passenger | null>(null);
  const [selectedPassengers, setSelectedPassengers] = useState<Set<number>>(new Set());
  
  // 判断乘客是否被筛选选中
  const isFiltered = (passengerId: number) => {
    return filteredPassengerIds.length === 0 || filteredPassengerIds.includes(passengerId);
  };
  
  // 获取点的颜色和样式
  const getPointStyle = (passenger: Passenger) => {
    const filtered = isFiltered(passenger.PassengerId);
    
    if (!filtered) {
      // 未被筛选选中的乘客：灰色，不可点击
      return {
        fill: '#9ca3af', // 灰色
        cursor: 'default',
        opacity: 0.5
      };
    }
    
    // 被筛选选中的乘客：根据生存状态显示颜色
    return {
      fill: passenger.Survived ? '#10b981' : '#ef4444',
      cursor: 'pointer',
      opacity: 1
    };
  };
  
  const handlePointClick = (passenger: Passenger) => {
    if (!isFiltered(passenger.PassengerId)) return; // 未被筛选选中的乘客不可点击
    
    const newSelected = new Set(selectedPassengers);
    if (newSelected.has(passenger.PassengerId)) {
      newSelected.delete(passenger.PassengerId);
    } else {
      newSelected.add(passenger.PassengerId);
    }
    setSelectedPassengers(newSelected);
    onPassengerClick?.(passenger);
    onPassengerSelect?.(Array.from(newSelected));
  };
  
  const handlePointHover = (passenger: Passenger | null) => {
    if (passenger && !isFiltered(passenger.PassengerId)) return; // 未被筛选选中的乘客不显示悬停
    
    setHoveredPassenger(passenger);
    onPassengerHover?.(passenger);
  };
  
  return (
    <div className="relative">
      <svg width={width} height={height}>
        {layout.map((point, index) => {
          const style = getPointStyle(point.passenger);
          const isSelected = selectedPassengers.has(point.passenger.PassengerId);
          
          return (
            <circle
              key={index}
              cx={point.x}
              cy={point.y}
              r={isSelected ? point.size * 1.5 : point.size} // 选中时放大
              fill={style.fill}
              opacity={style.opacity}
              cursor={style.cursor}
              stroke={isSelected ? '#3b82f6' : 'none'} // 选中时显示蓝色边框
              strokeWidth={isSelected ? 2 : 0}
              className="transition-all duration-200"
              onMouseEnter={() => handlePointHover(point.passenger)}
              onMouseLeave={() => handlePointHover(null)}
              onClick={() => handlePointClick(point.passenger)}
            />
          );
        })}
      </svg>
      
      {/* 乘客信息提示框 */}
      {hoveredPassenger && (
        <PassengerTooltip passenger={hoveredPassenger} />
      )}
    </div>
  );
};
```

#### 均匀分布布局实现（第一阶段）
```typescript
const useScatterLayout = (
  passengers: Passenger[], 
  containerWidth: number, 
  containerHeight: number, 
  layoutType: 'grid' | 'cluster' = 'grid'
): ScatterPoint[] => {
  return useMemo(() => {
    if (layoutType === 'grid') {
      return createGridLayout(passengers, containerWidth, containerHeight);
    }
    // cluster布局将在第二阶段实现
    return createGridLayout(passengers, containerWidth, containerHeight);
  }, [passengers, containerWidth, containerHeight, layoutType]);
};

const createGridLayout = (data: Passenger[], containerWidth: number, containerHeight: number): ScatterPoint[] => {
  if (data.length === 0) return [];
  
  const pointsPerRow = Math.ceil(Math.sqrt(data.length));
  const spacingX = containerWidth / (pointsPerRow + 1);
  const spacingY = containerHeight / (pointsPerRow + 1);
  
  return data.map((passenger, index) => {
    const row = Math.floor(index / pointsPerRow);
    const col = index % pointsPerRow;
    
    return {
      passenger,
      x: (col + 0.5) * spacingX,
      y: (row + 0.5) * spacingY,
      size: 6,
      isFiltered: true // 初始状态都为筛选选中
    };
  });
};
```

#### FilterPanel 组件
```tsx
interface FilterPanelProps {
  filters: FilterState;
  onFilterChange: (filterKey: keyof FilterState, value: any) => void;
  onClearAll: () => void;
}

const FilterPanel: React.FC<FilterPanelProps> = ({
  filters,
  onFilterChange,
  onClearAll
}) => {
  return (
    <div className="bg-white p-4 rounded-lg shadow-sm">
      <div className="flex justify-between items-center mb-4">
        <h3 className="font-semibold">筛选条件</h3>
        <button 
          onClick={onClearAll}
          className="text-sm text-blue-600 hover:text-blue-800"
        >
          清除全部
        </button>
      </div>
      
      {/* 生存状态过滤器 */}
      <FilterSection title="生存状态">
        <div className="flex gap-2">
          {[
            { value: 'survived', label: '生存', color: 'green' },
            { value: 'died', label: '死亡', color: 'red' }
          ].map(option => (
            <FilterChip
              key={option.value}
              label={option.label}
              color={option.color}
              selected={filters.survivalStatus.includes(option.value)}
              onToggle={() => {
                const newStatus = filters.survivalStatus.includes(option.value)
                  ? filters.survivalStatus.filter(s => s !== option.value)
                  : [...filters.survivalStatus, option.value];
                onFilterChange('survivalStatus', newStatus);
              }}
            />
          ))}
        </div>
      </FilterSection>
      
      {/* 其他过滤器实现类似 */}
      
      {/* 年龄范围滑块 */}
      <FilterSection title="年龄范围">
        <RangeSlider
          min={0}
          max={80}
          value={filters.ageRange}
          onChange={(value) => onFilterChange('ageRange', value)}
        />
      </FilterSection>
      
      {/* 搜索框 */}
      <FilterSection title="姓名搜索">
        <input
          type="text"
          placeholder="搜索乘客姓名..."
          value={filters.nameSearch}
          onChange={(e) => onFilterChange('nameSearch', e.target.value)}
          className="w-full p-2 border rounded"
        />
      </FilterSection>
    </div>
  );
};
```

## 开发步骤

### 第一阶段：基础架构搭建
1. **创建组件结构**
   - 创建PassengerScatterPlot组件
   - 创建FilterPanel组件
   - 创建SurvivalStats统计组件
   - 创建SurvivalAnalysisPage容器组件

2. **实现均匀分布布局**
   - 实现网格布局算法
   - 添加筛选状态显示逻辑
   - 实现基础交互功能

3. **集成数据接口**
   - 定义Passenger数据类型
   - 实现数据加载逻辑
   - 测试组件数据流

### 第二阶段：过滤器实现
1. **基础过滤器组件**
   - 实现多选过滤器组件
   - 添加滑块过滤器
   - 实现搜索框功能

2. **过滤器状态管理**
   - 实现筛选逻辑
   - 添加实时更新机制
   - 实现清除筛选功能

### 第三阶段：生存率统计系统
1. **统计计算逻辑**
   - 实现当前条件生存率计算
   - 实现总体生存率计算
   - 添加对比差异计算

2. **统计组件开发**
   - 创建SurvivalStats组件
   - 实现统计指标显示
   - 添加生存率可视化进度条

3. **实时更新机制**
   - 集成统计组件到主页面
   - 实现筛选条件变化时的实时更新
   - 添加统计数据的缓存优化

### 第四阶段：交互优化
1. **点阵交互增强**
   - 实现鼠标悬停提示
   - 添加点击选中功能
   - 实现框选操作

2. **性能优化**
   - 添加防抖处理
   - 优化渲染性能
   - 实现懒加载机制

### 第五阶段：高级功能（可选）
1. **聚类算法实现**
   - 集成Python预处理结果
   - 实现聚类布局切换
   - 添加聚类参数调节

2. **用户体验优化**
   - 添加动画效果
   - 实现响应式设计
   - 添加加载状态指示

### 第六阶段：测试和部署
1. **功能测试**
   - 单元测试
   - 集成测试
   - 用户测试

2. **部署上线**
   - 代码优化
   - 性能测试
   - 生产部署

## 技术要点

### 性能优化
- 使用React.memo优化组件重渲染
- 使用useMemo缓存计算结果
- 虚拟化技术处理大量数据点
- 防抖处理搜索输入

### 用户体验
- 实时过滤反馈
- 平滑的动画过渡
- 直观的筛选状态指示
- 键盘导航支持

### 可访问性
- 支持屏幕阅读器
- 键盘操作支持
- 高对比度模式
- 焦点管理

## 预期效果

新的生存分析页面将提供：
- 更直观的数据可视化：每个乘客都可见
- 强大的数据探索能力：多维度精细筛选
- 更好的交互体验：类似淘宝搜索的过滤器界面
- 更深入的数据洞察：通过聚类发现隐藏模式

这个设计将显著提升用户对泰坦尼克号乘客数据的理解深度和探索乐趣。